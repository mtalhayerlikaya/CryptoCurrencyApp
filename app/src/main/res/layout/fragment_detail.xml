<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_detail_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.detail.DetailFragment">

    <ProgressBar
        android:id="@+id/progressBarDetail"
        style="?android:attr/progressBarStyle"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/fragment_detail_root"
        app:layout_constraintEnd_toEndOf="@+id/fragment_detail_root"
        app:layout_constraintStart_toEndOf="@+id/fragment_detail_root"
        app:layout_constraintStart_toStartOf="@+id/fragment_detail_root"
        app:layout_constraintTop_toTopOf="@+id/fragment_detail_root" />


</androidx.constraintlayout.widget.ConstraintLayout>